var documenterSearchIndex = {"docs":
[{"category":"section","location":"analyses/#Analyses","page":"Analyses","text":"Every analysis takes an MNACircuit and returns a solution object that is read by name. sol[:vout] means the same thing in DC, transient, AC and noise — a node voltage, a branch current, or a device observable — so a readout written once survives a change of analysis.\n\nusing Cadnip\nusing Cadnip.MNA: MNACircuit, nameat, terminal_currents, op_vars\n\nlowpass = MNACircuit(sp\"\"\"\n* loaded RC low-pass, driven for DC, AC and transient at once\nV1 in 0 DC 1 AC 1 SIN(0 1 100k)\nR1 in out 1k\nR2 out 0 3k\nC1 out 0 1n\n\"\"\")\nnothing # hide","title":"Analyses"},{"category":"section","location":"analyses/#DC-operating-point-—-dc!","page":"Analyses","text":"op = dc!(lowpass)\nop[:out], op[:I_v1], op.converged\n\nNode voltages are named after the net, branch currents through a voltage source are :I_<source>, and :gnd (or node 0) is 0.0. sol.converged says whether Newton actually reached tolerance — worth checking before trusting a number from a circuit that might not have solved.","title":"DC operating point — dc!"},{"category":"section","location":"analyses/#The-operating-point-is-enumerable","page":"Analyses","text":"sort(collect(keys(op)))\n\nkeys, values, pairs, haskey and get all work, so an operating point can be collected into a Dict, diffed against another, or exported without knowing the node names up front:\n\nDict(pairs(op))","title":"The operating point is enumerable"},{"category":"section","location":"analyses/#Terminal-currents","page":"Analyses","text":"The solution vector cannot tell you a device's terminal current: KCL has already summed every device that meets at a node. Devices therefore report their own terminal currents as they stamp the converged point. Positive is into the device.\n\nterminal_currents(op)\n\nop[:i_m1_d]        # drain current of M1 — not inferred from a supply branch\nop[:i_r1_p]        # current into R1's first terminal\n\nBuilt-in devices report at their stamp, and so does every Verilog-A model: the stamp codegen accumulates one current per port over every branch that reaches it, so PDK and user models are covered without per-model configuration.","title":"Terminal currents"},{"category":"section","location":"analyses/#Operating-point-variables","page":"Analyses","text":"Alongside them the models report the small-signal quantities they computed — gm, gds, vth, vdsat — which no amount of reading the solution vector recovers. A Verilog-A model contributes every variable it declares with a desc/units attribute, which is how CMC and SPICE3-derived models already spell their .op output:\n\nop[:m1_gm]                     # transconductance of M1, from the model itself\nop[:m1_vds] > op[:m1_vdsat]    # in saturation, in the model's own numbers\nop_vars(op)                    # every device variable, as name => value\n\nBoth channels are gathered on the rebuild dc! already does at the converged point, and are absent from the fast restamping path, so transient simulation pays nothing for them.","title":"Operating-point variables"},{"category":"section","location":"analyses/#Transient-—-tran!","page":"Analyses","text":"tran!(circuit, tspan) returns a SciML solution. It is dense in time: index it with nameat(sol, :name, t), or call it as sol(t) for the whole state.\n\nsol = tran!(lowpass, (0.0, 20e-6))\n[nameat(sol, :out, t) for t in (0.0, 5e-6, 20e-6)]\n\nThe default solver is Sundials' IDA on the DAE form, configured for circuits (KLU sparse linear solver, extra error-test and Newton retries). Any SciML DAE, ODE or DDE algorithm can be passed instead, and tran! dispatches the problem type accordingly:\n\nsol = tran!(circuit, (0.0, 1e-6); solver=Rodas5P())       # ODE (mass matrix) path\nsol = tran!(circuit, (0.0, 1e-6); abstol=1e-12, reltol=1e-9)\n\nabstol also accepts a per-class NamedTuple, so node voltages, branch currents and charge states each get their natural scale instead of one scalar being dominated by the tiniest-unit variable:\n\nsol = tran!(circuit, (0.0, 1e-6); abstol=(vntol=1e-6, iabstol=1e-12, chgtol=1e-14))\n\nBreakpoints of PWL, PULSE and SIN sources are derived automatically and handed to the integrator as tstops, so it lands on source edges instead of discovering them through rejected steps (auto_tstops=false disables this).\n\nInitialization follows SPICE: transient sources are evaluated at t = 0 and a DC steady state is solved there before stepping.","title":"Transient — tran!"},{"category":"section","location":"analyses/#AC-small-signal-—-ac!","page":"Analyses","text":"ac! linearizes about the DC operating point and returns an ACSol carrying the descriptor state-space system and the Hz grid you asked for. Frequencies are in hertz, as in SPICE .ac; acdec(n, fstart, fstop) builds a log grid of n points per decade.\n\nf  = acdec(50, 1e3, 1e7)\nac = ac!(lowpass, f)\n\nresp = ac[:out]                  # complex response over the grid\nmag  = magnitude_db(ac, :out)    # dB\nphs  = phase_deg(ac, :out)       # degrees\n\ni3 = findfirst(<(mag[1] - 3), mag)\nf[i3]                            # first grid point past 1/(2π·(R1∥R2)·C1) = 212 kHz\n\nWithout a grid, evaluate at arbitrary angular frequencies (rad/s — the ControlSystems convention) instead:\n\nfreqresp(ac!(lowpass), :out, 2π .* [1e3, 1e5])\n\nFor ControlSystems / DescriptorSystems interop, subsystem(ac, :name) returns the SISO descriptor system, ready for ss, bode, poles and zeros:\n\nusing RobustAndOptimalControl                    # ss(::DescriptorStateSpace)\nmag, phase, w = bode(ss(subsystem(ac, :out)), 2π .* f)","title":"AC small-signal — ac!"},{"category":"section","location":"analyses/#Noise-—-noise!","page":"Analyses","text":"noise!(circuit, output; freqs) computes the output-referred noise PSD at output over a Hz grid, decomposed per source. It reuses the AC linearization: one adjoint solve (jωC + G)ᵀ x = e_out per frequency gives the transfer from every noise source at once, so the cost is one solve per frequency regardless of how many sources the circuit has.\n\nns = noise!(lowpass, :out; freqs=acdec(10, 1e3, 1e8))\n\nns[:onoise][1]                   # output PSD in V²/Hz at the first frequency\n\ntotal_noise(ns)                  # band-integrated, V rms\n\nThat total is the textbook sqrt(kT/C) of the load capacitor — the resistors set both the noise density and the bandwidth, and the two cancel.\n\nSources register themselves during that rebuild, so each is named after the device it came from and can be read on its own:\n\ncollect(keys(ns.contributions))\n\nThe registered sources today are resistor Johnson–Nyquist noise, diode shot noise, MOSFET channel thermal noise and flicker noise for the built-in devices, plus every white_noise/flicker_noise a Verilog-A model declares — which lights up the SPICE3 noise models of the VADistiller library (resistor, diode, BJT, MOS1/2/3/6/9, JFET, MESFET, BSIM3/4) and any PDK or user model.\n\nNaming a voltage source as the input refers the noise back to it. The unit-voltage transfer comes out of the same adjoint solve, so this costs nothing extra:\n\nns = noise!(lowpass, :out; freqs=acdec(10, 1e3, 1e8), input=:V1)\nns[:inoise][1], total_noise(ns; referred=:input)","title":"Noise — noise!"},{"category":"section","location":"analyses/#Sweeping-an-analysis","page":"Analyses","text":"dc! and tran! accept a CircuitSweep and return a SweepResult of (params, sol) pairs — see Parameters and sweeps.\n\ndivider = sp\"\"\"\n* divider with a swept top resistor\n.param r=1k\nV1 in 0 DC 1\nR1 in out r\nR2 out 0 1k\n\"\"\"\n\nfor (p, sol) in dc!(CircuitSweep(divider, Sweep(r = [1e3, 2e3, 4e3])))\n    println(p.r, \" → \", sol[:out])\nend","title":"Sweeping an analysis"},{"category":"section","location":"analyses/#Interactive-exploration","page":"Analyses","text":"With a Makie backend loaded, Cadnip.explore(circuit, tspan) opens a transient plot with one logarithmic slider per scalar circuit parameter and re-simulates live as you drag:\n\nusing GLMakie\nCadnip.explore(MNACircuit(lowpass_builder; R1=1e3), (0.0, 20e-6))","title":"Interactive exploration"},{"category":"section","location":"devices/#Devices-and-models","page":"Devices and models","text":"","title":"Devices and models"},{"category":"section","location":"devices/#Built-in-devices","page":"Devices and models","text":"Cadnip stamps the primitives itself. These need no model package and no .model card:\n\nSPICE line Device\nR1 a b 1k resistor\nC1 a b 1n capacitor\nL1 a b 1m inductor\nV1 a b DC 5 AC 1 independent voltage source (DC and AC excitation)\nI1 a b DC 1m independent current source\nV1 a b PWL(0 0 1u 1) / PULSE(...) / SIN(...) time-dependent sources\nE/G/H/F lines voltage- and current-controlled sources (VCVS, VCCS, CCVS, CCCS)\nX1 a b sub subcircuit instance\n\nSources take a DC value, an AC magnitude and phase, and a transient waveform on the same line; each analysis reads the part that applies to it, so one netlist serves dc!, ac! and tran! without editing.\n\nV1 in 0 DC 1.2 AC 1 SIN(1.2 5m 1meg)","title":"Built-in devices"},{"category":"section","location":"devices/#Model-cards-and-the-two-tier-lookup","page":"Devices and models","text":"Anything with a .model card — diodes, MOSFETs, BJTs, JFETs — resolves through two tiers:\n\nTier 1, the registry. Level-dispatched built-in and library models. A model package adds methods to Cadnip.ModelRegistry.getmodel, so loading the package is all it takes for the matching .model card to resolve:\n\nusing Cadnip\nusing Cadnip.MNA: MNACircuit\nusing VADistillerModels        # registers the SPICE3 model library\n\namp = MNACircuit(sp\"\"\"\n* NMOS common-source stage\n.model nch nmos level=1 vto=0.7 kp=100u lambda=0.01\nVdd vdd 0 DC 5\nVin gate 0 DC 1.1472\nM1 drain gate 0 0 nch w=20u l=1u\nRd vdd drain 10k\n\"\"\")\n\nop = dc!(amp)\nop[:drain], op[:i_m1_d], op[:m1_gm]\n\nVADistillerModels claims the SPICE3 model cards: nmos/pmos at levels 1, 2, 3, 6, 9, BSIM3v3 (levels 8/49) and BSIM4v8 (levels 14/54), npn/pnp (Gummel–Poon), njf/pjf (levels 1/2), and d. The library carries more models than it registers cards for (MESFET, passives), reachable by name from the netlist scope. Other model packages in this repository register their own cards — PSPModels (PSP103, JUNCAP200), CMCModels, VACASKModels, PhotonicModels.\n\nThe :r, :c and :l cards resolve to Cadnip's own primitives. The :d card deliberately does not: MNA.Diode is an incomplete reference implementation (no cjo, m, bv, tt), so a diode model card needs a Tier-1 provider such as VADistillerModels rather than silently getting a partial device.\n\nTier 2, the netlist scope. PDK-specific and custom Verilog-A devices come from the netlist's own directives — .hdl \"foo.va\", .include \"foo.sp\", .lib \"foo.sp\" section, and their jlpkg://Package/path forms. The most recent include wins.","title":"Model cards and the two-tier lookup"},{"category":"section","location":"devices/#Verilog-A-models","page":"Devices and models","text":"Verilog-A is the supported way to add a device. Cadnip compiles the module into the same stamping code its built-in devices use, so a VA device is a first-class device: it takes part in DC, AC, transient and noise, contributes terminal currents and operating-point variables, and is differentiable.\n\nva\"\"\"\nmodule VAResistor(p, n);\n    parameter real R = 1000.0;\n    inout p, n;\n    electrical p, n;\n    analog I(p,n) <+ V(p,n)/R;\nendmodule\n\"\"\"\nnothing # hide\n\nwarning: Disciplines are implicit\nelectrical, V() and I() are built into the parser. Do not include \"disciplines.vams\" — it triggers parser bugs.\n\nnote: ForwardDiff must be loadable\nThe code generated for a Verilog-A module imports ForwardDiff (it is what carries the derivatives through the device equations), so ForwardDiff has to be resolvable in the environment that loads the model — add it to the project that contains your va\"...\" or .hdl-including netlist.\n\nFrom a netlist, a .hdl directive brings the module into scope and the module name becomes the device's model name:\n\n.hdl \"myresistor.va\"\nN1 a b varesistor r=2k\n\nTwo model features are picked up from ordinary Verilog-A declarations, with nothing extra to configure:\n\na module-level real/integer with a desc/units attribute is an operating-point variable, reported as op[:m1_gm] and friends:\n(* desc = \"Transconductance\", units = \"S\" *) real gm;\nwhite_noise(pwr) and flicker_noise(pwr, exp) register noise sources named <instance>_<label>, which noise! then decomposes by name. The call itself evaluates to zero in the time domain, and the whole registration folds away on the transient path.","title":"Verilog-A models"},{"category":"section","location":"devices/#PDKs-as-packages","page":"Devices and models","text":"A PDK author bakes netlist and Verilog-A content into a package at build time:\n\nCadnip.precompile_pdk(@__MODULE__, \"pdk.spice\")\nCadnip.precompile_va(@__MODULE__, \"device.va\")\n\nUsers then reference the baked content from their netlist, and pay no parse or compile cost for it at run time:\n\n.lib \"jlpkg://MyPDK/models/corners.sp\" typical\n\nPDK modules are baremodules so that SPICE names like inv, log and exp cannot collide with Julia's; generated code spells out Base. for anything it needs from Base.","title":"PDKs as packages"},{"category":"section","location":"devices/#Custom-devices-in-Julia","page":"Devices and models","text":"Below the netlist layer, a device is a type with a stamp! method that writes its contributions into an MNAContext. This is Cadnip's internal API — it is what the built-in devices and the Verilog-A codegen both target — and is documented in the repository's doc/mna_architecture.md and doc/mna_ad_stamping.md.\n\nusing Cadnip.MNA\n\nfunction rc(params, spec, t=0.0; x=Float64[], ctx=MNAContext())\n    reset_for_restamping!(ctx)\n    src = get_node!(ctx, :src)\n    out = get_node!(ctx, :out)\n    stamp!(VoltageSource(params.vin; name=:V1), ctx, src, 0)\n    stamp!(Resistor(params.R), ctx, src, out)\n    stamp!(Capacitor(params.C), ctx, out, 0)\n    return ctx\nend\n\ndc!(MNACircuit(rc; vin=1.0, R=1e3, C=1e-9))[:out]\n\nA builder written this way is an ordinary MNACircuit builder: sweeps, alter and every analysis work on it. What it does not get is the netlist's parameter bookkeeping — it declares no parameter names, so overrides are not checked against it, and it has to read params.<name> itself.\n\nFor a custom device meant to be used from a netlist, prefer Verilog-A: it gets model-card parameter handling, noise, operating-point variables and terminal currents for free.","title":"Custom devices in Julia"},{"category":"section","location":"devices/#ModelingToolkit-components","page":"Devices and models","text":"With ModelingToolkit loaded, @declare_MSLConnector wraps an MTK component with electrical pins into a Cadnip device, so a model developed and validated in the MTK ecosystem can be dropped into a SPICE circuit:","title":"ModelingToolkit components"},{"category":"macro","location":"devices/#Cadnip.@declare_MSLConnector-devices","page":"Devices and models","text":"@declare_MSLConnector(mtk_model, pin_ports...)\n\nnote: For this to be used ModelingToolkit must be loaded\nCadnip itself only provides a stub-defination of this type. The full implementation is in the Cadnip-ModelingToolkit extension module. Which is automatically loaded if Cadnip and ModelingToolkit are both loaded.\n\nDefined the functions needed to connect a MTK based model (defined using MSL Pins) to Cedar. As input provide the model (an ODESystem), and a list of pins defined using ModelingToolkitStandardLibary.Electrical.Pins. These pins can be as direct components of the model or subcomponents other components. When you use this component as a subcircuit (as shown in the example) they be connected to Cadnip AbstractNets corresponding to the SPICE nodes, in the order you list them.\n\nNote that the model does not have to be (and usually won't be) solvable in MTK – it can be incomplete and unablanced. The remaining variables coming from the rest of the circuit, e.g. as defined using SPICE. The usual way to develop this would be to initially write the model in MTK using MSL, then delete all the voltage/current sources and declare that the places they were connected are port pins usng this macro.\n\nThis is a higher level version of the DAECompiler.@declare_MTKConnector and does, in the end, return a subtype of MTKConnector.\n\nThis means it is struct with a constructor that you can override the parameters to by keyword argument. You can check what parameters are available by using parameternames on an instance of the type. The struct will have call overriden (i.e. it will be a functor) to allow the connections Cadnip exposes to all be hooked up.\n\nIt is used for example as:\n\n@mtkmodel Foo begin\n    @parameters begin\n        param=0.0\n        ...\n    end\n    @components begin\n        Pos = Pin()\n        Neg = Pin()\n        ...\n    end\n    ...\nend\n\nfoo = foo(name=:foo1)\nconst FooConn = @declare_MSLConnector(foo, foo.Pos, foo.Neg)\ncircuit = sp\"\"\" ...\nXfoo 1 0 $(FooConn(param = 42.0))\n...\n\"\"\"e\n\n\n\n\n\n","title":"Cadnip.@declare_MSLConnector"},{"category":"section","location":"netlists/#Loading-circuits","page":"Loading circuits","text":"Cadnip is a compiler as much as a simulator. A netlist is parsed, resolved (sema), and turned into a Julia builder function — a function that stamps the circuit's contributions into an MNA context. Everything downstream (dc!, tran!, ac!, noise!, sweeps, differentiation) works on that function, so the loading step is where a netlist stops being text and becomes code.\n\nMNACircuit pairs a builder with a set of parameters. All the entry points below produce one.","title":"Loading circuits"},{"category":"section","location":"netlists/#From-a-string","page":"Loading circuits","text":"The shortest way in. sp\"...\" is SPICE, spc\"...\" is Spectre, va\"...\" is Verilog-A:\n\nusing Cadnip\nusing Cadnip.MNA: MNACircuit\n\ndivider = MNACircuit(sp\"\"\"\n* divider\nV1 vcc 0 DC 5\nR1 vcc out 1k\nR2 out 0 1k\n\"\"\")\n\ndc!(divider)[:out]\n\nThey are macros, so the netlist is compiled when the surrounding code is — no parse cost at run time, and no world-age tax, because nothing is eval'd.\n\nnote: The first line of a SPICE deck is its title\nsp\"...\" parses a complete deck, and SPICE decks open with a title line, so the first line is not read as a circuit element. That is what the * divider line above is for. For a snippet with no title of its own, pass the i (inline) flag — sp\"...\"i.Getting this wrong is silent: drop the title and the V1 card is eaten as one, leaving a circuit with no source that solves to all zeros and has no I_v1 to read — see the transcript in the README.\n\nThe same circuit in Spectre syntax. Spectre has no title line, so spc\"...\" needs no flag:\n\ndivider_scs = MNACircuit(spc\"\"\"\nv1 (vcc 0) vsource type=dc dc=5\nr1 (vcc out) resistor r=1k\nr2 (out 0) resistor r=1k\n\"\"\")\n\ndc!(divider_scs)[:out]","title":"From a string"},{"category":"section","location":"netlists/#From-a-file","page":"Loading circuits","text":"For anything larger than a snippet, keep the netlist in a file. The language is inferred from the extension: .scs is Spectre, anything else is SPICE.\n\ncircuit = MNACircuit(\"amp.sp\")      # SPICE\ncircuit = MNACircuit(\"amp.scs\")     # Spectre\nsol = dc!(circuit)\n\nFor performance-sensitive code, define the builder at module top level instead. Base.include on a SpiceFile / SpectreFile evaluates the generated code into your module, binding a function named after the file:\n\nBase.include(@__MODULE__, SpiceFile(\"amp.sp\"))   # defines `amp(params, spec, ...)`\n\nc = MNACircuit(amp; R1=1e3)\nsol = dc!(c)\n\nA netlist string that only exists at run time (read from a database, generated by a script) goes through the string form of MNACircuit, which parses and evals on the spot:\n\ncircuit = MNACircuit(read(\"amp.sp\", String); lang=:spice, source_dir=@__DIR__)\n\nsource_dir is what relative .include / .hdl paths inside such a string resolve against; a netlist loaded from a file resolves them against its own directory.","title":"From a file"},{"category":"section","location":"netlists/#Loading-happens-at-top-level","page":"Loading circuits","text":"Two separate rules, both of which come down to loading a netlist where Julia can see the result.\n\nwarning: Runtime parsing defines a builder, so mind the world age\nMNACircuit(path) and MNACircuit(code; lang=...) parse the netlist and Base.eval a builder function for it. Julia freezes the world age of a top-level statement while it runs, so the fresh builder can only be called from a later statement:circuit = MNACircuit(\"amp.sp\")   # statement 1: defines the builder\nsol = dc!(circuit)               # statement 2: calls it — fine\n\ndc!(MNACircuit(\"amp.sp\"))        # ✗ same statement: MethodError, \"method too new\"A function body freezes its world age at entry the same way, so building and solving inside one call fails for the same reason. Load the deck at top level, and the function is free to build and solve as it likes:Base.include(@__MODULE__, SpiceFile(\"amp.sp\"))   # top level: defines `amp`\n\nfunction run_sim()\n    c = MNACircuit(amp; R1=1e3)                  # no eval, no world-age tax\n    dc!(c)\nend\n\nnote: A netlist macro is not\nsp\"...\", spc\"...\" and va\"...\" compile their deck when the macro expands, which happens before the enclosing code runs — so the builder always predates any call to it, and the macro leaves nothing at the call site but the builder. No world-age tax, and they go wherever an expression goes, a function body included:function run_sim()\n    dc!(MNACircuit(sp\"\"\"\n    * divider\n    V1 vcc 0 DC 5\n    R1 vcc out 1k\n    R2 out 0 1k\n    \"\"\"))[:out]\nend\nrun_sim()Each expansion gets a deck module of its own, so two decks in one function that both define .subckt divider do not collide.\n\nInput Loader\nSPICE file MNACircuit(\"amp.sp\")\nSpectre file MNACircuit(\"amp.scs\")\nDeck as a top-level definition Base.include(@__MODULE__, SpiceFile(\"amp.sp\"))\nSPICE string sp\"\"\"...\"\"\" or MNACircuit(code; lang=:spice)\nSpectre string spc\"\"\"...\"\"\" or MNACircuit(code; lang=:spectre)\nVerilog-A string va\"\"\"...\"\"\"\nAlready-compiled builder MNACircuit(my_builder; R=1e3)","title":"Loading happens at top level"},{"category":"section","location":"netlists/#Subcircuits","page":"Loading circuits","text":".subckt definitions are instantiated with X lines, and their internal nodes are reachable in the flattened name table with the instance name as a prefix:\n\ntop = MNACircuit(sp\"\"\"\n* divider instantiated as a subcircuit\n.subckt divider a b\nR1 a mid 1k\nR2 mid b 1k\n.ends\nV1 vcc 0 DC 6\nX1 vcc 0 divider\n\"\"\")\n\nop = dc!(top)\nop[:x1_mid]\n\nInstance parameters on the X line, and the .param defaults a .subckt declares, are covered in Parameters and sweeps.","title":"Subcircuits"},{"category":"section","location":"netlists/#A-deck-is-a-namespace","page":"Loading circuits","text":"A netlist file is a deck, and each loaded deck gets a Julia module of its own; only the builder is bound in your module. Two decks that each define .subckt divider therefore do not overwrite each other:\n\nBase.include(@__MODULE__, SpiceFile(\"amp_a.sp\"))   # its own .subckt divider\nBase.include(@__MODULE__, SpiceFile(\"amp_b.sp\"))   # a different .subckt divider\n\ndc!(MNACircuit(amp_a))   # each answers for its own deck\ndc!(MNACircuit(amp_b))\n\nTwo sp\"...\" strings in the same local scope are the exception: a module cannot be defined in expression position, so that case is ordinary Julia redefinition in a scope you wrote.","title":"A deck is a namespace"},{"category":"section","location":"netlists/#Include-directives-and-libraries","page":"Loading circuits","text":"The usual SPICE directives pull in external content, and paths resolve relative to the including file:\n\n.include \"models.sp\"\n.lib \"corners.sp\" typical\n.hdl \"mydevice.va\"\n\nA jlpkg://Package/path URL resolves inside an installed Julia package, which is how a PDK shipped as a package is referenced:\n\n.lib \"jlpkg://MyPDK/models/corners.sp\" typical\n\nAnything a directive brings in lands in the netlist's scope, which is the second of the two tiers device names resolve through — see Model cards and the two-tier lookup.","title":"Include directives and libraries"},{"category":"section","location":"#Cadnip.jl","page":"Home","text":"Circuit Analysis & Differentiable Numerical Integration Program\n\nCadnip is an analog circuit simulator written in Julia. It reads SPICE and Spectre netlists and Verilog-A models, compiles them into Julia code, and solves them with a Modified Nodal Analysis (MNA) engine on top of the SciML solver stack. Because the whole simulator is ordinary Julia, a circuit is differentiable, sweepable, and composable with the rest of the ecosystem.","title":"Cadnip.jl"},{"category":"section","location":"#Installation","page":"Home","text":"Cadnip is in the General registry:\n\nusing Pkg\nPkg.add(\"Cadnip\")\n\nCadnip itself provides the passives, sources, and the analyses. Transistor and diode model cards (.model nch nmos level=1, .model dmod d, …) resolve through model packages — the ones this repository ships live under models/ (VADistillerModels, PSPModels, CMCModels, …) and are loaded alongside Cadnip with using.","title":"Installation"},{"category":"section","location":"#A-first-circuit","page":"Home","text":"A netlist is the input, and the same sol[:name] readout serves every analysis:\n\nusing Cadnip\nusing Cadnip.MNA: MNACircuit, nameat\n\ndivider = MNACircuit(sp\"\"\"\n* resistive divider\nV1 vcc 0 DC 5\nR1 vcc out 1k\nR2 out 0 1k\n\"\"\")\n\nsol = dc!(divider)\nsol[:out]\n\nnote: Two namespaces\nCadnip exports the analyses and the sweep API (dc!, tran!, ac!, noise!, alter, Sweep, CircuitSweep, acdec, …) and the netlist macros. The circuit and device layer lives in the Cadnip.MNA submodule, so MNACircuit and friends are imported from there — using Cadnip.MNA: MNACircuit, nameat, as above.\n\nSomething with a time constant, driven by a step:\n\nrc = MNACircuit(sp\"\"\"\n* RC low-pass driven by a 0 → 1 V step at t = 1 µs\nV1 in 0 PWL(0 0 1u 0 1.001u 1)\nR1 in out 1k\nC1 out 0 1n\n\"\"\")\n\nsol = tran!(rc, (0.0, 20e-6))\n[nameat(sol, :out, t) for t in (0.0, 2e-6, 20e-6)]   # before the step, mid-rise, settled\n\nand the same circuit in the frequency domain — the pole sits at 1/(2π·1 kΩ·1 nF) ≈ 159 kHz:\n\nlowpass = MNACircuit(sp\"\"\"\n* RC low-pass, AC excitation\nV1 in 0 DC 0 AC 1\nR1 in out 1k\nC1 out 0 1n\n\"\"\")\n\nac = ac!(lowpass, acdec(10, 1e3, 1e7))\nmagnitude_db(ac, :out)[1:3]     # flat in the passband","title":"A first circuit"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Page What it covers\nLoading circuits netlist files, string macros, subcircuits, include/PDK directives\nParameters and sweeps .param overrides, alter, sweep axes, continuation\nAnalyses dc!, tran!, ac!, noise! and how to read their results\nDevices and models built-in devices, model cards, Verilog-A, custom devices\n\nDesign notes and internals live in the doc/ directory of the repository — doc/mna_architecture.md and doc/code_tour_mna_pipeline.md are the entry points for the compiler and solver pipeline.","title":"Where to go next"},{"category":"section","location":"parameters/#Parameters-and-sweeps","page":"Parameters and sweeps","text":"A netlist .param is the knob a design is parameterized on: a bias voltage, a device size, a source amplitude, a process corner. Cadnip treats those knobs as the circuit's parameters — override one at construction, re-bind it with alter, or make it a sweep axis, using the same name in every case.","title":"Parameters and sweeps"},{"category":"section","location":"parameters/#Overriding-a-.param","page":"Parameters and sweeps","text":"using Cadnip\nusing Cadnip.MNA: MNACircuit\n\namp = sp\"\"\"\n* parameterized divider\n.param rtop=1k\n.param rbot=1k\nV1 vcc 0 DC 6\nR1 vcc out rtop\nR2 out 0 rbot\n\"\"\"\n\nc = MNACircuit(amp; rtop=2e3)      # override at construction\ndc!(c)[:out]\n\nalter returns a new circuit with a knob re-bound. It introduces the knob if the circuit was not carrying it yet, so a .param need not be seeded at construction time:\n\nc2 = alter(c; rbot=3e3)\ndc!(c2)[:out]\n\nOverrides reach every place the netlist uses the parameter: DC source values, device values, .model card parameters, and the runtime-evaluated amplitudes and frequencies of SIN/PULSE sources, at top level or inside a subcircuit.","title":"Overriding a .param"},{"category":"section","location":"parameters/#Hierarchy:-a-leaf-is-a-parameter,-a-group-is-an-instance","page":"Parameters and sweeps","text":"A scope — the top level, or a subcircuit instance — has parameters of its own and children it instantiates, in two namespaces that can collide. One rule tells them apart: a leaf is a parameter of the scope, a group is a child.\n\ntop = sp\"\"\"\n* divider as a subcircuit, with an instance parameter\n.subckt divider a b r1val=1k\nR1 a mid r1val\nR2 mid b 1k\n.ends\nV1 vcc 0 DC 6\nX1 vcc 0 divider r1val=2k\n\"\"\"\n\nc = MNACircuit(top; x1=(r1val=1e3,))    # a group → the instance X1\ndc!(c)[:x1_mid]\n\nAn override outranks the instance line: X1 ... r1val=2k in the netlist still leaves r1val reachable from alter and from a sweep axis. A dotted selector addresses the same place and is what you use for a sweep axis name:\n\ndc!(alter(c; var\"x1.r1val\"=4e3))[:x1_mid]\n\nWhen a name is both a parameter and an instance (a .param x1 next to an X1 instance), the shape decides: x1=2.0 sets the parameter, x1=(r1val=…) addresses the instance, and params=(x1=2.0,) names the parameter explicitly when you need both at once.","title":"Hierarchy: a leaf is a parameter, a group is an instance"},{"category":"section","location":"parameters/#Names-that-reach-nothing-throw","page":"Parameters and sweeps","text":"An override that names nothing used to be silent, which reads exactly like a parameter with no effect — a swept design would come out as a flat curve rather than an error. Names are now checked against what the netlist declares:\n\ntry\n    MNACircuit(amp; rtopp=2e3)          # typo\ncatch err\n    println(sprint(showerror, err))\nend\n\nTwo things are genuinely not reachable this way, and both throw at construction:\n\ndevice instance parameters (r1=(r=2e3,), m1=(w=…)) — parameterize the netlist with a .param and override that instead;\na name no scope declares — i.e. a typo, as above.\n\nA hand-written builder function declares no names, so its parameters are not checked.","title":"Names that reach nothing throw"},{"category":"section","location":"parameters/#Corners-are-just-parameters","page":"Parameters and sweeps","text":"A .model card can read a .param, so a process corner is an ordinary sweep axis rather than a second netlist:\n\n.param vt0=0.7\n.model nch nmos level=1 vto=vt0 kp=100u\n\ndc!(CircuitSweep(amp, Sweep(vt0 = [0.6, 0.7, 0.8])))","title":"Corners are just parameters"},{"category":"section","location":"parameters/#Sweeps","page":"Parameters and sweeps","text":"Don't hand-roll a loop that rebuilds a circuit per value. Wrap the range in a sweep, bind it to a builder with CircuitSweep, and run the analysis: dc! and tran! return a SweepResult that iterates (params, sol) pairs, with each parameter point aligned to its solution.\n\nsweep = CircuitSweep(amp, Sweep(rtop = [1e3, 2e3, 4e3]))\n\nfor (p, sol) in dc!(sweep)\n    println(p.rtop, \" Ω → \", sol[:out], \" V\")\nend\n\nKeyword arguments to CircuitSweep set the base value of everything the sweep does not vary; a swept axis needs no seeding of its own.\n\ncs = CircuitSweep(ce_amplifier, Sweep(vac = [0.5e-3, 1e-3, 2e-3]); freq=1e3)\nfor (params, sol) in tran!(cs, (0.0, 5e-3))\n    # ...\nend\n\nAxes combine in three ways:\n\nConstructor Meaning\nProductSweep(a=…, b=…) full grid: every a with every b\nTandemSweep(a=…, b=…) zipped: a[i] with b[i]\nSerialSweep(s1, s2) one sweep after the other\n\nSweep(a=…, b=…) with several keywords is a product sweep. Hierarchical axes use the same dotted selectors as alter:\n\nCircuitSweep(top, Sweep(var\"x1.r1val\" = [1e3, 2e3]))","title":"Sweeps"},{"category":"section","location":"parameters/#DC-sweeps-continue","page":"Parameters and sweeps","text":"dc!(::CircuitSweep) warm-starts each point from the previous converged operating point, the way a SPICE .dc sweep does. Adjacent points are usually a small perturbation of each other, so the warm start lands inside Newton's quadratic-convergence basin and costs markedly fewer iterations — on a 40-junction ladder over 60 points, 477 Newton iterations continued against 877 cold.\n\nresult = dc!(CircuitSweep(circuit, sweep))                      # continued (default)\nresult = dc!(CircuitSweep(circuit, sweep); continuation=false)  # independent points\n\nA point that fails to converge is never used as a starting guess, a guess of the wrong length (a point that changed the system size) is dropped, and the GMIN/source-stepping fallbacks restart from zeros — so a bad guess costs iterations, never a solution. Pass continuation=false when following a branch is exactly what you don't want, e.g. on a circuit with multiple DC solutions.\n\nA single operating point can be warm-started by hand with the same seam:\n\nsol  = dc!(circuit)\nwarm = dc!(alter(circuit; rtop=1.01e3); u0=sol.x)\n\nTransient sweeps DC-initialize each point independently.","title":"DC sweeps continue"}]
}
